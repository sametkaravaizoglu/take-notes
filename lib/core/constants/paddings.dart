import 'package:flutter/material.dart';

class Paddings {
  static const verticalXS = EdgeInsets.symmetric(vertical: 4);
  static const verticalS = EdgeInsets.symmetric(vertical: 8);
  static const verticalM = EdgeInsets.symmetric(vertical: 12);
  static const verticalL = EdgeInsets.symmetric(vertical: 16);

  static const horizontalXS = EdgeInsets.symmetric(horizontal: 4);
  static const horizontalS = EdgeInsets.symmetric(horizontal: 8);
  static const horizontalM = EdgeInsets.symmetric(horizontal: 12);
  static const horizontalL = EdgeInsets.symmetric(horizontal: 16);

  static const allXS = EdgeInsets.all(4);
  static const allS = EdgeInsets.all(8);
  static const allM = EdgeInsets.all(12);
  static const allL = EdgeInsets.all(16);

  static const topXS = EdgeInsets.only(top: 4);
  static const topS = EdgeInsets.only(top: 8);
  static const topM = EdgeInsets.only(top: 12);
  static const topL = EdgeInsets.only(top: 16);
}
